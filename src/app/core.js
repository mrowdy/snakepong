define(['app/log', 'app/event', 'app/dom', 'app/classlist'], function(Log, Event, Dom, Classlist){

    return {
        log: Log,
        event: Event,
        dom: Dom,
        classlist: Classlist
    }

});